<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             xmlns:local="clr-namespace:SDKBrowserMaui.Examples.ChatControl.FeaturesCategory.AttachmentsExample"
             x:Class="SDKBrowserMaui.Examples.ChatControl.FeaturesCategory.AttachmentsTemplatesExample.AttachmentsTemplates">
	<ContentView.Resources>
		<ResourceDictionary>
			<local:ItemConverter x:Key="ItemConverter" />
			<!-- >> chat-attachments-view-templates -->
			<Style TargetType="telerik:ChatMessageAttachmentView">
				<Setter Property="ControlTemplate">
					<Setter.Value>
						<ControlTemplate>
							<telerik:RadBorder BorderColor="Black" BorderThickness="1" CornerRadius="5">
								<HorizontalStackLayout Spacing="5" Padding="2">
									<Image>
										<Image.Source>
											<FontImageSource FontFamily="{Static telerik:TelerikFont.Name}"
															 Glyph="{Static telerik:TelerikFont.IconFile}"
															 Size="16"
															 Color="Black" />
										</Image.Source>
									</Image>
									<Button Text="Download"
											Command="{Binding DownloadAttachmentsCommand, Source={RelativeSource AncestorType={Type telerik:RadChat}}}"
											CommandParameter="{TemplateBinding Attachment}" />
									<Button Text="Share"
											Command="{Binding ShareAttachmentsCommand, Source={RelativeSource AncestorType={Type telerik:RadChat}}}"
											CommandParameter="{TemplateBinding Attachment}" />
									<Label Text="{TemplateBinding Attachment.FileName}"
										   VerticalTextAlignment="Center"/>
								</HorizontalStackLayout>
							</telerik:RadBorder>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
			<!-- << chat-attachments-view-templates -->
			
			<!-- >> chat-attachments-actionsview-templates -->
			<Style TargetType="telerik:ChatMessageAttachmentActionsView">
				<Setter Property="ControlTemplate">
					<Setter.Value>
						<ControlTemplate>
							<telerik:RadBorder BackgroundColor="#00796B" CornerRadius="5">
								<HorizontalStackLayout Spacing="5" Padding="2">
									<Button Text="Download"
											Command="{Binding DownloadAttachmentsCommand, Source={RelativeSource AncestorType={Type telerik:RadChat}}}"
											CommandParameter="{TemplateBinding Attachment}" />
									<Button Text="Share"
											Command="{Binding ShareAttachmentsCommand, Source={RelativeSource AncestorType={Type telerik:RadChat}}}"
											CommandParameter="{TemplateBinding Attachment}" />
								</HorizontalStackLayout>
							</telerik:RadBorder>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
			<!-- << chat-attachments-actionsview-templates -->
		</ResourceDictionary>
	</ContentView.Resources>
	<!-- >> chat-attachments-templates -->
	<telerik:RadChat x:Name="chat"
					 IsMoreButtonVisible="True"
					 AutoGenerateMoreButtonActions="True"
				 	 IsSpeechToTextButtonVisible="{OnPlatform Default=true, WinUI=false}"
					 ItemConverter="{StaticResource ItemConverter}"
					 ItemsSource="{Binding Items}"
					 Message="{Binding Message}"
				 	 SendMessageCommand="{Binding SendMessageCommand}"
					 AttachedFileConverter="{Static local:AttachedFileConverter.Instance}"
					 AttachedFilesSource="{Binding AttachedFiles}"
					 AttachFilesCommand="{Binding AttachFilesCommand, Converter={Static local:AttachFilesCommandConverter.Instance}}" />
	<!-- << chat-attachments-templates -->
</ContentView>
