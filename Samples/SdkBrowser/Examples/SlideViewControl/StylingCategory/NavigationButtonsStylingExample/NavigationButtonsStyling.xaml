<?xml version="1.0" encoding="utf-8" ?>
<telerik:RadContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                        xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
                        x:Class="SDKBrowserMaui.Examples.SlideViewControl.StylingCategory.NavigationButtonsStylingExample.NavigationButtonsStyling"
                        Margin="5">
    <telerik:RadContentView.Resources>
        <ResourceDictionary>
            <!-- >> slideview-navigation-styling-resources -->
            <Style x:Key="NavigationButtonStyle" TargetType="telerik:RadButton">
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="TextColor" Value="Red"/>
                <Setter Property="BackgroundColor" Value="Yellow"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
            <!-- << slideview-navigation-styling-resources -->
        </ResourceDictionary>
    </telerik:RadContentView.Resources>
    <Grid>
        <!-- >> slideview-navigation-styling-xaml -->
        <telerik:RadSlideView x:Name="slideView"
                              InteractionMode="Pan"
                              NavigationButtonsVisibility="Visible"
                              NavigateToNextItemButtonStyle="{StaticResource NavigationButtonStyle}"
                              NavigateToPreviousItemButtonStyle="{StaticResource NavigationButtonStyle}">
        <!-- << slideview-navigation-styling-xaml -->
            <Grid>
                <Button Text="Item 0" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
            <Grid>
                <Button Text="Item 1" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
            <Grid>
                <Button Text="Item 2" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
            <Grid>
                <Button Text="Item 3" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
            <Grid>
                <Button Text="Item 4" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
            <Grid>
                <Button Text="Item 5" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>
        </telerik:RadSlideView>
    </Grid>
</telerik:RadContentView>