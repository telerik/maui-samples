<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             x:Class="SDKBrowserMaui.Examples.DropDownButtonControl.StylingCategory.DropDownStylingExample.DropDownStyling">
    <ContentView.Resources>
        <Style TargetType="Label" x:Key="LabelStyle">
            <Setter Property="HorizontalOptions" Value="Start" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Margin" Value="0, 0, 0, 8" />
        </Style>
        <Style TargetType="telerik:RadDropDownButton">
            <Setter Property="Margin" Value="0, 0, 0, 24" />
        </Style>
    </ContentView.Resources>
    <VerticalStackLayout HorizontalOptions="{OnIdiom Default=Fill, Desktop=Start}"
                         VerticalOptions="Start"
                         WidthRequest="{OnPlatform WinUI=320, MacCatalyst=320}">
        <!-- >> dropdownbutton-dropdownstyling-appearance -->
        <telerik:RadDropDownButton Content="Click to open drop-down"
                                   DropDownBackgroundColor="#FFF6E5"
                                   DropDownBorderColor="#FF6358"
                                   DropDownBorderThickness="1"
                                   DropDownCornerRadius="12">
            <telerik:RadDropDownButton.DropDownContent>
                <Label Text="Custom background, border and corner radius." 
                        Padding="12" />
            </telerik:RadDropDownButton.DropDownContent>
        </telerik:RadDropDownButton>
        <!-- << dropdownbutton-dropdownstyling-appearance -->
    </VerticalStackLayout>
</ContentView>
