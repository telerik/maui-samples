<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             xmlns:local="clr-namespace:SDKBrowserMaui.Examples.CircularSliderControl.StylingCategory.LabelsStyleSelectorExample"
             x:Class="SDKBrowserMaui.Examples.CircularSliderControl.StylingCategory.LabelsStyleSelectorExample.LabelsStyleSelector">
    <ContentView.Resources>
        <!-- >> circularslider-labelsstyleselector-selector -->
        <local:RangeLabelStyleSelector x:Key="RangeLabelStyleSelector">
            <local:RangeLabelStyleSelector.InsideRangeStyle>
                <Style TargetType="Label">
                    <Setter Property="TextColor" Value="#018383" />
                    <Setter Property="FontFamily" Value="Arial" />
                    <Setter Property="FontSize" Value="16" />
                </Style>
            </local:RangeLabelStyleSelector.InsideRangeStyle>
            <local:RangeLabelStyleSelector.OutsideRangeStyle>
                <Style TargetType="Label">
                    <Setter Property="TextColor" Value="#830183" />
                    <Setter Property="FontFamily" Value="Arial" />
                    <Setter Property="FontSize" Value="16" />
                </Style>
            </local:RangeLabelStyleSelector.OutsideRangeStyle>
        </local:RangeLabelStyleSelector>
        <!-- << circularslider-labelsstyleselector-selector -->
    </ContentView.Resources>
    <Grid>
        <!-- >> circularslider-labelsstyleselector-xaml -->
        <telerik:RadCircularSlider Minimum="0"
                                   Maximum="100"
                                   Value="35"
                                   LabelsPlacement="End"
                                   LabelStep="10"
                                   LabelStyleSelector="{StaticResource RangeLabelStyleSelector}"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Start"
                                   MinimumHeightRequest="300" />
        <!-- << circularslider-labelsstyleselector-xaml -->
    </Grid>
</ContentView>
