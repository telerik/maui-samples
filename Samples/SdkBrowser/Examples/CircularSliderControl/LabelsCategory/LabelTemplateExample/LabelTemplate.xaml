<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             x:Class="SDKBrowserMaui.Examples.CircularSliderControl.LabelsCategory.LabelTemplateExample.LabelTemplate">
    <ContentView.Resources>
        <!-- >> circularslider-labels-labeltemplate -->
        <DataTemplate x:Key="CustomLabelTemplate">
            <Label Text="{Binding}"
                   FontSize="16"
                   TextColor="#674BB2" />
        </DataTemplate>
        <!-- << circularslider-labels-labeltemplate -->
    </ContentView.Resources>
    <Grid>
        <!-- >> circularslider-labels-labeltemplate-xaml -->
        <telerik:RadCircularSlider Minimum="-100"
                                   Maximum="100"
                                   Value="35"
                                   LabelsPlacement="End"
                                   LabelStep="20"
                                   LabelTemplate="{StaticResource CustomLabelTemplate}"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Start"
                                   MinimumHeightRequest="300" />
        <!-- << circularslider-labels-labeltemplate-xaml -->
    </Grid>
</ContentView>
