<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerik="http://schemas.telerik.com/2022/xaml/maui"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             xmlns:views="clr-namespace:QSF.Views"
             xmlns:converters="clr-namespace:QSF.Converters"
             xmlns:qsf="clr-namespace:QSF.Pages"
             ios:Page.UseSafeArea="False"
             xmlns:telerikMaui="clr-namespace:Telerik.Maui;assembly=Telerik.Maui.Core"
             NavigationPage.HasNavigationBar="False"
             x:Name="home"
             x:Class="QSF.Pages.MainPageMobile">

    <ContentPage.Resources>
        <converters:StatusTagToStringConverter x:Key="StatusTagToStringConverter" NewValue="New" UpdatedValue="Upd" BetaValue="Beta" PreviewValue="Prev" />

        <DataTemplate x:Key="ControlsCollectionViewHeaderTemplate">
            <Grid RowDefinitions="Auto, Auto"
                  Margin="0, 0, 0, 10">
                <views:HighlightsView />
                <Grid Grid.Row="1">
                    <Label Text="Components"
                           HorizontalOptions="Start"
                           VerticalOptions="End"
                           FontSize="{OnPlatform Default=22, Android=24}" />
                    <telerik:RadButton Command="{Binding NavigateToDownloadTrialCommand}"
                                       HorizontalOptions="End"
                                       Style="{StaticResource MoreInfoButtonStyle}"
                                       MinimumWidthRequest="{OnPlatform iOS=120}"
                                       MinimumHeightRequest="{OnPlatform iOS=44}"
                                       AutomationId="moreInfoButton" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ControlsCollectionViewItemTemplate">
            <Grid ColumnDefinitions="Auto, *"
                  ColumnSpacing="14"
                  BackgroundColor="{DynamicResource ItemBackgroundColor}"
                  Padding="14, 8">
                <telerik:RadBorder WidthRequest="44"
                                   CornerRadius="4"
                                   BackgroundColor="{DynamicResource IconBackgroundColor}">
                    <Label Text="{Binding Icon}"
                           TextColor="{DynamicResource AccentColor8}"
                           FontSize="24"
                           FontFamily="TelerikControlsIcons"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center" />
                </telerik:RadBorder>
                <Grid Grid.Column="1"
                      RowDefinitions="Auto, *"
                      VerticalOptions="Center">
                    <Label Text="{Binding DisplayName}"
                           VerticalOptions="Center"
                           Style="{StaticResource ControlNameLabelStyle}" 
                           AutomationId="controlName" />
                    <Label Grid.Row="1"
                           Text="{Binding ShortDescription}"
                           Style="{StaticResource DescriptionLabelWithTruncationStyle}" 
                           AutomationId="controlDescription" />
                </Grid>
                <HorizontalStackLayout Grid.ColumnSpan="2"
                                        HorizontalOptions="End"
                                        VerticalOptions="Start"
                                        Spacing="2">
                    <telerik:RadBorder Style="{StaticResource StatusTagBorderStyle}"
                                       BackgroundColor="{Binding Status, Converter={StaticResource StatusTagToColorConverter}}"
                                       AutomationId="statusTag">
                        <Label Text="{Binding Status, Converter={StaticResource StatusTagToStringConverter}}"
                               Style="{StaticResource StatusTagLabelStyle}" />
                    </telerik:RadBorder>
                    <telerik:RadBorder BackgroundColor="{DynamicResource BadgeAIBackgroundColor}"
                                       Style="{StaticResource StatusTagBorderStyle}"
                                       IsVisible="{Binding HasAICapability}"
                                       AutomationId="aiTag">
                        <Grid ColumnDefinitions="Auto,Auto"
                              ColumnSpacing="2"
                              HorizontalOptions="Center">
                            <Label Grid.Column="0"
                                   FontFamily="{x:Static telerikMaui:TelerikFont.Name}"
                                   Text="{x:Static telerikMaui:TelerikFont.IconSparkle}"
                                   FontSize="10"
                                   TextColor="{DynamicResource TextOnAccentColor}"
                                   LineHeight="1"
                                   VerticalOptions="Fill"
                                   VerticalTextAlignment="Center"
                                   HorizontalTextAlignment="Center"/>
                            <Label Grid.Column="1"
                                   Text="AI"
                                   Style="{StaticResource StatusTagLabelStyle}"
                                   LineHeight="1"
                                   VerticalOptions="Fill"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"/>
                        </Grid>
                    </telerik:RadBorder>
                </HorizontalStackLayout>
            </Grid>
        </DataTemplate>

        <Style x:Key="ControlsCollectionViewItemViewStyle" TargetType="telerik:RadCollectionViewItemView" BasedOn="{StaticResource RadCollectionViewItemStyle}">
            <Setter Property="BorderThickness" Value="0, 0, 0, 1" />
            <Setter Property="BorderColor" Value="#33B8B8B8" />
            <Setter Property="BackgroundColor" Value="{DynamicResource ItemBackgroundColor}" />
        </Style>
    </ContentPage.Resources>

    <views:QSFPageContentView BackgroundColor="{DynamicResource DefaultBackgroundColor}" AutomationId="mainPageMobile">
        <views:QSFPageContentView.HeaderControlTemplate>
            <ControlTemplate>
                <Grid ColumnDefinitions="Auto, *, Auto"
                      IgnoreSafeArea="True">
                    <Image Style="{StaticResource HeaderImageStyle}" />
                    <Label Grid.Column="1"
                           Style="{StaticResource HeaderLabelStyle}"
                           Text="{Binding AppTitle}"
                           FontSize="{OnIdiom Default=14, Desktop=18}"
                           Margin="10, 0"
                           AutomationId="titleLabel" />
                    <HorizontalStackLayout Grid.Column="2">
                        <ImageButton Clicked="Settings_Clicked"
                                     Source="settings.png"
                                     WidthRequest="35"
                                     HeightRequest="35"
                                     Aspect="Center"
                                     HorizontalOptions="EndAndExpand"
                                     VerticalOptions="Center"
                                     BackgroundColor="Transparent"
                                     AutomationId="settingsButton" />
                        <ImageButton Clicked="Search_Clicked"
                                     Source="searchheader.png"
                                     WidthRequest="35"
                                     HeightRequest="35"
                                     Aspect="Center"
                                     HorizontalOptions="EndAndExpand"
                                     VerticalOptions="Center"
                                     BackgroundColor="Transparent" 
                                     AutomationId="searchButton" />
                        <ImageButton Command="{Binding NavigateToThemeSettingsCommand}"
                                     CommandParameter="{Binding Source={x:Reference home}}"
                                     Source="{StaticResource ThemesButtonFontImageSource}"
                                     Style="{StaticResource PageAppBarImageButtonStyle}"
                                     AutomationId="themeSettingsButton" />
                    </HorizontalStackLayout>
                </Grid>
            </ControlTemplate>
        </views:QSFPageContentView.HeaderControlTemplate>
        <views:QSFPageContentView.BodyControlTemplate>
            <ControlTemplate>
                <Grid>
                    <telerik:RadCollectionView x:Name="controlsCollectionView"
                                           Grid.Row="1"
                                           HeaderTemplate="{StaticResource ControlsCollectionViewHeaderTemplate}"
                                           ItemTemplate="{StaticResource ControlsCollectionViewItemTemplate}"
                                           ItemsSource="{Binding Controls}"
                                           ItemTapped="controlsCollectionView_ItemTapped"
                                           ItemViewStyle="{DynamicResource ControlsCollectionViewItemViewStyle}"
                                           SelectionMode="None"
                                           Margin="16, 16, 16, 0"
                                           AutomationId="controlsCollectionView">
                        <telerik:RadCollectionView.ItemsLayout>
                            <telerik:CollectionViewLinearLayout ItemLength="96" />
                        </telerik:RadCollectionView.ItemsLayout>
                    </telerik:RadCollectionView>
                </Grid>
            </ControlTemplate>
        </views:QSFPageContentView.BodyControlTemplate>
    </views:QSFPageContentView>
</ContentPage>